<?xml version="1.0" encoding="UTF-8"?>

<!-- Logback configuration. See http://logback.qos.ch/manual/index.html -->
<configuration scan="true" scanPeriod="10 seconds">
    <include resource="org/springframework/boot/logging/logback/base.xml" />
	<jmxConfigurator/>
    <!--定义日志文件的存储地址和前缀名-->
    <property name="LOG_HOME" value="c:/test/benz-bmw-game-new" />
    <property name="LOG_PREFIX" value="benz-bmw-game-new" />

    <!-- 一般信息按照每天生成日志文件 -->
    <appender name="INFO_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <File>${LOG_HOME}/${LOG_PREFIX}-info.log</File>
        <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
            <!-- 每天一归档 -->
            <fileNamePattern>${LOG_HOME}/${LOG_PREFIX}-info-%d{yyyyMMdd}-%i.log</fileNamePattern>
            <!-- 单个日志文件最多500MB, 30天的日志周期，最大不能超过20GB -->
            <maxFileSize>100MB</maxFileSize>
            <maxHistory>30</maxHistory>
            <totalSizeCap>20GB</totalSizeCap>
        </rollingPolicy>
        <encoder>
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
            <Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} -%msg%n</Pattern>
        </encoder>
        <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
            <level>DEBUG</level>
        </filter>
    </appender>

    <!-- 异步输出 -->
         <appender name ="ASYNC" class= "ch.qos.logback.classic.AsyncAppender">
             <!-- 不丢失日志.默认的,如果队列的80%已满,则会丢弃TRACT、DEBUG、INFO级别的日志 -->
             <discardingThreshold>0</discardingThreshold>
             <!-- 更改默认的队列的深度,该值会影响性能.默认值为256 -->
             <queueSize>256</queueSize>
             <!-- 添加附加的appender,最多只能添加一个 -->
             <appender-ref ref ="INFO_FILE"/>
         </appender>

    <!--错误信息按照每天生成日志文件-->
    <!--<appender name="ERROR_FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">-->
        <!--<File>${LOG_HOME}/${LOG_PREFIX}-error.log</File>-->
        <!--<rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">-->
            <!--&lt;!&ndash; 每天一归档 &ndash;&gt;-->
            <!--<fileNamePattern>${LOG_HOME}/${LOG_PREFIX}-error-%d{yyyyMMdd}.log.%i</fileNamePattern>-->
            <!--&lt;!&ndash; 单个日志文件最多500MB, 30天的日志周期，最大不能超过20GB &ndash;&gt;-->
            <!--<maxFileSize>100MB</maxFileSize>-->
            <!--<maxHistory>30</maxHistory>-->
            <!--<totalSizeCap>20GB</totalSizeCap>-->
        <!--</rollingPolicy>-->
        <!--<encoder>-->
            <!--&lt;!&ndash;格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符&ndash;&gt;-->
            <!--<Pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} -%msg%n</Pattern>-->
        <!--</encoder>-->
        <!--<filter class="ch.qos.logback.classic.filter.ThresholdFilter">-->
            <!--<level>ERROR</level>-->
        <!--</filter>-->
    <!--</appender>-->

    <!--<appender name="LOGSTASH" class="net.logstash.logback.appender.LogstashTcpSocketAppender">-->
        <!--<destination>172.20.100.219:5044</destination>-->
        <!--<encoder charset="UTF-8" class="net.logstash.logback.encoder.LogstashEncoder">-->
            <!--<customFields>{"appname":"hundred-cattle-new-server"}</customFields>-->
        <!--</encoder>-->
    <!--</appender>-->


    <!--开发环境，测试环境-->
    <springProfile name="test">
        <root level="INFO">
            <appender-ref ref="ASYNC"/>
            <!--<appender-ref ref="ERROR_FILE"/>-->
        </root>
    </springProfile>

    <!--预发布环境-->
    <springProfile name="prerelease,prod">
        <root level="INFO">
            <appender-ref ref="ASYNC"/>
            <!--<appender-ref ref="ERROR_FILE"/>-->
            <!--<appender-ref ref="LOGSTASH"/>-->
        </root>
    </springProfile>

</configuration>